import React from 'react';
import Modal from "../Modal"
interface Props {
  // Define your props here
}

const CreateAgendaModal = (props: Props) => {
  return (
    <Modal>
      
    </Modal>
  );
};

export default CreateAgendaModal;